// lib/screens/lancamento_massa_screen.dart
import 'package:flutter/material.dart';

class LancamentoMassaScreen extends StatelessWidget {
  const LancamentoMassaScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Lançamento em Massa')),
      body: const Center(
        child: Text('Tela de Lançamento em Massa (A ser implementada)'),
      ),
    );
  }
}
