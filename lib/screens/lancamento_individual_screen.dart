// lib/screens/lancamento_individual_screen.dart
import 'package:flutter/material.dart';

class LancamentoIndividualScreen extends StatelessWidget {
  const LancamentoIndividualScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Lançamento Individual')),
      body: const Center(
        child: Text('Tela de Lançamento Individual (A ser implementada)'),
      ),
    );
  }
}
