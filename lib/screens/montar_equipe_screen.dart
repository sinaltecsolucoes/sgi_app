// lib/screens/montar_equipe_screen.dart
import 'package:flutter/material.dart';

class MontarEquipeScreen extends StatelessWidget {
  const MontarEquipeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Montagem de Equipe')),
      body: const Center(
        child: Text('Tela de Montagem de Equipes (A ser implementada)'),
      ),
    );
  }
}
